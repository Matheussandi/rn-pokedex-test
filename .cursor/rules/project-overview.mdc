---
description: Visão geral do projeto Pokédex Explorer
alwaysApply: true
---

# Pokédex Explorer

App React Native (Expo 56) que consome a PokéAPI GraphQL v1beta2.

## Stack

- Expo Router para navegação (tabs + stack)
- TypeScript strict
- Apollo Client com codegen (`yarn codegen`)
- AsyncStorage para favoritos locais
- `@expo/vector-icons` (Ionicons) para todos os ícones

## API

- Endpoint: `https://graphql.pokeapi.co/v1beta2`
- Rate limit ~100 req/h — usar `cache-first` e paginação
- Pode ficar indisponível — sempre tratar loading/erro

## Estrutura

- `src/app/` — rotas finas (apenas re-exportam features)
- `src/features/<nome>/` — MVVM (pokemon-model, pokemon-view, index)
- `src/graphql/operations/` — queries `.graphql`
- `src/graphql/generated/` — tipos gerados (não editar manualmente)
- `src/lib/` — utilitários compartilhados

## Convenções

- Código simples e objetivo
- Sem lógica de negócio em `src/app/`
- Nomes de Pokémon capitalizados na UI

## Regras relacionadas

Consulte a regra específica conforme a área que estiver tocando:

- [mvvm-pattern](mdc:.cursor/rules/mvvm-pattern.mdc) — padrão MVVM das features (model/view/index)
- [graphql-apollo](mdc:.cursor/rules/graphql-apollo.mdc) — queries `.graphql`, codegen e Apollo Client
- [testing](mdc:.cursor/rules/testing.mdc) — estratégia e convenções de testes (Jest + RNTL)
- [expo-icons](mdc:.cursor/rules/expo-icons.mdc) — uso de `@expo/vector-icons` para ícones

## Documentação do projeto

- `README.md` — como rodar, scripts e estrutura
- `TECHNICAL_TEST.md` — briefing original do teste técnico

## Documentação externa

- [Expo (SDK 56)](https://docs.expo.dev/) e [Expo Router](https://docs.expo.dev/router/introduction/)
- [Apollo Client](https://www.apollographql.com/docs/react/) e [GraphQL Code Generator](https://the-guild.dev/graphql/codegen)
- [PokéAPI GraphQL](https://pokeapi.co/docs/graphql) — endpoint `v1beta2`
- [React Native Testing Library](https://callstack.github.io/react-native-testing-library/)
